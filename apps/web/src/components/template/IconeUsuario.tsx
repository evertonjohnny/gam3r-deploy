import { IconUser } from "@tabler/icons-react";

export default function IconeUsuario() {
	return (
		<div className="hidden md:flex w-12 h-12 rounded-full p-1 items-center justify-center bg-violet-dark text-white">
			<IconUser size={34} stroke={1.3} />
		</div>
	);
}
